@extends('layouts.app')
@section('title', 'Mes services | GeoArtisans')
@section('content')
<section class="dashboard-shell"><span class="eyebrow">ESPACE ARTISAN / SERVICES</span><h1 class="page-title">Vos services,<br><em>clairement.</em></h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="dashboard-grid"><form method="POST" action="{{ route('artisan.services.store') }}" class="dashboard-card workspace-form">@csrf<h2>Ajouter un service</h2><label>Nom<input name="name" required></label><label>Description<textarea name="description" rows="4"></textarea></label><div class="form-grid"><label>Prix min.<input type="number" name="price_min" min="0"></label><label>Prix max.<input type="number" name="price_max" min="0"></label></div><button class="button button-dark" type="submit">Ajouter</button></form><div class="dashboard-card"><h2>Services publiés</h2>@forelse($profile?->services ?? [] as $service)<div class="line-item"><div><strong>{{ $service->name }}</strong><span>{{ $service->description }}</span></div><form method="POST" action="{{ route('artisan.services.destroy', $service) }}">@csrf @method('DELETE')<button class="icon-button" title="Supprimer" type="submit">×</button></form></div>@empty<p class="empty-state">Aucun service pour le moment.</p>@endforelse</div></div></section>
@endsection
