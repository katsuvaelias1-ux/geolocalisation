@extends('layouts.app')
@section('title', 'Mes réalisations | GeoArtisans')
@section('content')
<section class="dashboard-shell"><span class="eyebrow">ESPACE ARTISAN / RÉALISATIONS</span><h1 class="page-title">Montrez ce que<br><em>vous créez.</em></h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="dashboard-grid"><form method="POST" action="{{ route('artisan.portfolio.store') }}" enctype="multipart/form-data" class="dashboard-card workspace-form">@csrf<h2>Ajouter une réalisation</h2><label>Titre<input name="title" required></label><label>Description<textarea name="description" rows="4"></textarea></label><label>Image<input type="file" name="image" accept="image/*" required></label><button class="button button-dark" type="submit">Publier</button></form><div class="dashboard-card"><h2>Galerie</h2>@forelse($profile?->portfolioItems ?? [] as $item)<div class="line-item"><div><strong>{{ $item->title }}</strong><span>{{ $item->description }}</span></div><form method="POST" action="{{ route('artisan.portfolio.destroy', $item) }}">@csrf @method('DELETE')<button class="icon-button" title="Supprimer" type="submit">×</button></form></div>@empty<p class="empty-state">Aucune réalisation pour le moment.</p>@endforelse</div></div></section>
@endsection
