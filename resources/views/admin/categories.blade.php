@extends('layouts.app')
@section('title', 'Gestion des catégories | GeoArtisans')
@section('content')
<section class="dashboard-shell"><span class="eyebrow">ADMINISTRATION / CATÉGORIES</span><h1 class="page-title">Organiser les<br><em>métiers locaux.</em></h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="dashboard-grid"><form method="POST" action="{{ route('admin.categories.store') }}" class="dashboard-card workspace-form">@csrf<h2>Nouvelle catégorie</h2><label>Nom<input name="name" required></label><label>Slug<input name="slug" required></label><label>Description<textarea name="description" rows="3"></textarea></label><label>Icône<input name="icon"></label><button class="button button-dark" type="submit">Créer</button></form><div class="dashboard-card"><h2>Catégories actives</h2>@forelse($categories as $category)<div class="line-item"><div><strong>{{ $category->name }}</strong><span>{{ $category->artisan_profiles_count }} artisans · {{ $category->services_count }} services</span></div><form method="POST" action="{{ route('admin.categories.toggle', $category) }}">@csrf @method('PATCH')<button class="icon-button" title="Activer ou désactiver" type="submit">{{ $category->status ? '✓' : '○' }}</button></form></div>@empty<p class="empty-state">Aucune catégorie.</p>@endforelse</div></div>{{ $categories->links() }}</section>
@endsection
