@extends('layouts.app')
@section('title', 'Contact | GeoArtisans')
@section('content')
<section class="auth-shell"><div class="auth-aside"><span class="eyebrow">CONTACT / BUTEMBO</span><h1>Une question, une idée, un besoin.</h1><p>Écrivez à l’équipe GeoArtisans Butembo.</p></div><div class="form-panel"><span class="eyebrow">Votre message</span><h2>Parlons-nous.</h2>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('contact.store') }}" class="stack-form">@csrf<label>Nom<input name="name" value="{{ old('name') }}" required>@error('name')<small class="field-error">{{ $message }}</small>@enderror</label><label>E-mail<input type="email" name="email" value="{{ old('email') }}" required>@error('email')<small class="field-error">{{ $message }}</small>@enderror</label><label>Téléphone<input name="phone" value="{{ old('phone') }}"></label><label>Sujet<input name="subject" value="{{ old('subject') }}" required>@error('subject')<small class="field-error">{{ $message }}</small>@enderror</label><label>Message<textarea name="message" rows="5" required>{{ old('message') }}</textarea>@error('message')<small class="field-error">{{ $message }}</small>@enderror</label><button class="button button-dark button-full" type="submit">Envoyer le message ↗</button></form></div></section>
@endsection
