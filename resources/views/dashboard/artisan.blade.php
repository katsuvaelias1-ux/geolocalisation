@extends('layouts.app')
@section('title', 'Espace artisan | GeoArtisans')
@section('content')
<section class="dashboard-shell"><div class="dashboard-intro"><span class="eyebrow">ESPACE ARTISAN / 01</span><div class="account-heading">@if(auth()->user()->profile_photo)<img class="account-avatar" src="{{ auth()->user()->profilePhotoUrl() }}" alt="Photo de profil">@else<div class="account-avatar account-avatar-fallback">{{ strtoupper(substr(auth()->user()->first_name ?: auth()->user()->name, 0, 1)) }}</div>@endif<h1>Votre savoir-faire mérite d’être trouvé.</h1></div><p>Construisez votre présence locale et rendez vos services visibles.</p></div><div class="status-banner"><span class="status-dot"></span><div><strong>{{ $profile?->verification_status === 'verified' ? 'Profil vérifié' : 'Profil en attente' }}</strong><span>{{ $profile?->verification_status === 'verified' ? 'Votre profil est visible par les clients.' : 'Complétez votre profil, puis attendez la validation de l’administration.' }}</span></div></div><div class="dashboard-grid"><article class="dashboard-card dashboard-card-accent"><span class="card-index">.01</span><h2>Mon profil</h2><p>{{ $profile ? 'Votre profil artisan est créé.' : 'Présentez votre métier, votre zone et votre disponibilité.' }}</p><a class="button button-dark" href="{{ route('artisan.profile') }}">Configurer mon profil</a></article><article class="dashboard-card"><span class="card-index">.02</span><h2>Mes services</h2><strong class="metric">{{ $profile?->services_count ?? 0 }}</strong><span class="empty-state">services publiés</span><a class="text-link" href="{{ route('artisan.services') }}">Gérer mes services ↗</a></article><article class="dashboard-card"><span class="card-index">.03</span><h2>Mes avis</h2><strong class="metric">{{ $profile?->reviews_count ?? 0 }}</strong><span class="empty-state">avis reçus</span><a class="text-link" href="{{ route('artisan.messages') }}">Voir mes messages ↗</a></article></div></section>
@endsection
